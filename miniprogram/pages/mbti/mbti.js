Page({
  data: {
    // Because we are using localhost to debug, make sure the local server is running.
    // E.g. run `python3 -m http.server 8080` in `h5-mbti/` directory.
    url: 'http://127.0.0.1:8080'
  },
  onLoad: function (options) {
    // Do any initialization here
  }
})
